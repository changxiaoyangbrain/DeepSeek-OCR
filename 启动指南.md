/root/DeepSeek-OCR/
├── app_demo.py           # 新主入口 (434行) - 精简版 UI 组装
├── run_app_demo.sh       # 启动脚本 (模块化版本)
├── gradio_vllm_demo.py   # 原始单文件版本 (1922行) - 保留备用
│
└── app/                  # 模块目录 (总计1400行)
    ├── __init__.py       # 模块导出
    ├── config.py         # 配置: 路径、尺寸预设、Prompt模板 (120行)
    ├── utils.py          # 工具: 日志、正则、文本处理 (174行)
    ├── engine.py         # 引擎: LLM初始化与管理 (147行)
    ├── processors.py     # 处理: 图片/批量/PDF处理 (640行)
    └── ui_components.py  # 界面: CSS、HTML模板 (310行)


    模块	功能
config.py	SIZE_CONFIGS, TASK_PROMPTS, get_prompt()
utils.py	log_info/success/warning/error(), clean_output_text(), extract_grounding_references()
engine.py	init_llm(), restart_engine(), 单例LLM管理
processors.py	process_single_image(), process_batch_images(), process_pdf_document(), get_pdf_page_count()
ui_components.py	CUSTOM_CSS, HEADER_HTML, TIPS_HTML, FOOTER_HTML, PROMPT_CHOICES
PDF页面选择器
app_demo.py 中已添加 PDF 页面范围选择控件：

起始页 (pdf_page_start): Number 输入框，默认1
结束页 (pdf_page_end): Number 输入框，默认-1（全部）
页数显示 (pdf_page_info): 上传PDF后自动显示总页数
使用方式
两个版本功能完全相同，模块化版本更易于维护和扩展。

选项卡	内容
📄 Text	纯文本输出
🎨 Markdown	Markdown 渲染预览
🖼️ Boxes	每页的边界框标注图（Gallery）
✂️ Cropped Images	裁剪的图片区域（Gallery）
🔍 Raw	模型原始输出（含坐标标记）


# 运行模块化版本
./run_app_demo.sh

# 或运行原始单文件版本
./run_demo.sh